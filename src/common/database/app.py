# -*- coding: utf-8 -*-

from django.apps import AppConfig


class DatabaseConfig(AppConfig):
    """Configuration for users and permissions functionalties."""

    name = 'src.common.database'
    verbose_name = 'Database'
    verbose_name_plural = 'Database'
